/**
 * 
 * LIFO (LAST IN FIRST OUT)
 * e.g. 
 *      -- Undo/Redo
 *      -- History onject in Browser
 * 
 * IMPLEMENTATION
 * -- Array
 *      METHODS
 *      -- Push + Pop
 *      -- Unshift + Shift
 * -- Linked List
 * 
 * COMPLEXITIES
 * -- Insertion -> O(1)  Main Adv.
 * -- Removal -> O(1)   Main Adv
 * -- Searching -> O(N)
 * -- Access -> O(N)
 * 
 * 
 * -- Stacks are used to handle function invocation (the call stack), for operatrions like undo/redo , for routing (remember pages you have visited & go backward/forward) and much more.
 * -- They are not a built in DS in JS, but are relatively simple to implement.
 * 
 */
// Some changes
